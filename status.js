const Discord = require('discord.js');
const client = new Discord.Client();

let fs = require('fs');

module.exports = {
	name: 'status',
	description: 'status',
	aliases: ['status', '현황'],
	execute(msg, args) {

    },
};